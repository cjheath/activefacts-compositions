vocabulary BinaryObj1;

Beer is identified by its Name;
Person is identified by its Name;

Beer Preference is where
    Person prefers at most one preferred-Beer;

// Here, each Person has at most one Beer Preference, because it can only occur once in "Person prefers Beer"
