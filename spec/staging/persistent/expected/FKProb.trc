Summary of FKProb
LoadBatch
	LoadBatch->LoadBatch ID[PN1] as "Load Batch ID"
	LoadBatch->Start DateTime as "Start Date Time"
OT STG
	OT STG->Name[N1,P2.0] as "Name"
	OT STG->[LoadBatch]->LoadBatch ID[P2.1] as "Load Batch ID"
	OT STG->LoadTime as "Load Time"
	OT STG->OT ID as "OT ID"
	OT STG->RecordSource as "Record Source"
	OT STG->DOT?->VT? as "VT"
VTP STG
	VTP STG->[VT]->DOT->OT->Name[N1.0,P2.0] as "VT Name"
	VTP STG->Name[N1.1,P2.1] as "Name"
	VTP STG->[LoadBatch]->LoadBatch ID[P2.2] as "Load Batch ID"
	VTP STG->LoadTime as "Load Time"
	VTP STG->RecordSource as "Record Source"
	VTP STG->VTP ID as "VTP ID"
VTPRestriction STG
	VTPRestriction STG->[VT]->DOT->OT->Name[N1.0,P2.0] as "VT Name"
	VTPRestriction STG->[VTP]->VT->DOT->OT->Name[N1.1,P2.1] as "VTP VT Name"
	VTPRestriction STG->[VTP]->Name[N1.2,P2.2] as "VTP Name"
	VTPRestriction STG->[LoadBatch]->LoadBatch ID[P2.3] as "Load Batch ID"
	VTPRestriction STG->LoadTime as "Load Time"
	VTPRestriction STG->RecordSource as "Record Source"
	VTPRestriction STG->VTPRestriction ID as "VTP Restriction ID"
